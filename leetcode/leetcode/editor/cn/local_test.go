package leetcode

import "testing"

func TestXXX(t *testing.T) {
	t.Error(minArray([]int{3, 1, 3}))
}

func TestMap(t *testing.T) {
	mapping := make(map[int]bool)
	mapping[0] = true
	t.Error(mapping[1])
}
